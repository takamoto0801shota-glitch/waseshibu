# Google ログイン（Supabase Auth）セットアップ

## 1. Supabase プロジェクト作成

1. [Supabase](https://supabase.com) でプロジェクトを作成
2. **SQL Editor** で `supabase/schema.sql` を実行
3. **Project Settings → API** から URL と `anon` key をコピー

## 2. Google OAuth 設定

### Google Cloud Console

1. [Google Cloud Console](https://console.cloud.google.com/) → APIs & Services → Credentials
2. **OAuth 2.0 Client ID** を作成（Web application）
3. **Authorized redirect URIs** に追加:
   - `https://<your-project-ref>.supabase.co/auth/v1/callback`
4. Client ID / Client Secret をコピー

### Supabase Dashboard

1. **Authentication → Providers → Google** を有効化
2. Client ID / Client Secret を貼り付け
3. **Authentication → URL Configuration** で Site URL を設定:
   - 本番: `https://atlas-production-c8d3.up.railway.app`
   - 開発: `http://localhost:3000`
4. Redirect URLs に追加:
   - `https://atlas-production-c8d3.up.railway.app/auth/callback`
   - `http://localhost:3000/auth/callback`

## 3. 環境変数

**重要:** `.env.example` の `xxxxx` はプレースホルダーです。実際の値に置き換えてください。

Supabase Dashboard → **Project Settings → API** から取得:

```
NEXT_PUBLIC_SUPABASE_URL=https://abcdefghijklmnop.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

Railway で設定後、**必ず再デプロイ**（Variables 変更だけでは古いビルドが残ることがあります）:

```bash
railway variables set NEXT_PUBLIC_SUPABASE_URL=https://あなたのプロジェクト.supabase.co
railway variables set NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...
git push origin main
```

## 4. 動作確認

1. `/login` で「Googleでログイン」
2. 初回ログイン → `user_data` テーブルに行が作成される
3. オンボーディング完了後、データがクラウドに保存される
4. 再訪問時 → 自動ログイン + 前回の状態を復元
5. マイページ → 「Googleアカウントからログアウト」

## 保存データ（`user_data.app_state`）

| 項目 | 内容 |
|------|------|
| profile | 学年・モード・科目・やりたいこと |
| plan | 今日のプラン・勉強履歴（log） |
| plan.roadmapSteps | ロードマップ投影 |
| dailyRecords | 日別の勉強・解放時間 |
| currentBlock / sessionPhase | セッション途中の再開用 |
